import type { FirebaseOptions } from "firebase/app";
import type { Adapter } from "next-auth/adapters";
export interface FirestoreAdapterOptions {
    emulator?: {
        host?: string;
        port?: number;
    };
}
export declare function FirestoreAdapter({ emulator, ...firebaseOptions }: FirebaseOptions & FirestoreAdapterOptions): Adapter;
